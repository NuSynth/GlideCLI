Loop Desk Check:

start values:

reducedProjected.Count = 0;
projectedSimList.Count = 5;
sizeCheck = projectedSimList.Count; == 5
count = 0;
Current_Y = 1;


First Run of Loop:

reducedProjected.Count = 0;
projectedSimList.Count = 5;
sizeCheck = 5
count = 0;
Current_Y = 1;

while (count < projectedSimList.Count) : is 0 < 5? true
{
  GetReducedNew( count, sizeCheck);
  --sizeCheck;
  ++count;
}

GetReducedNew( count , sizeCheck )
{
  SimModel newSims = new SimModel();

  IF sizeCheck > Current_Y : is 5 > 1? true
  {
    newSims = projectedSimList[count];
    reducedProjected.Add(newSims);
  }
}

First Run of Loop Results:

WAS:
reducedProjected.Count = 0;
projectedSimList.Count = 5;
sizeCheck = 5
count = 0;
Current_Y = 1;

NOW:
reducedProjected.Count = 1;
projectedSimList.Count = 5;
sizeCheck = 4
count = 1;
Current_Y = 1;



Second Run of Loop:

reducedProjected.Count = 1;
projectedSimList.Count = 5;
sizeCheck = 4
count = 1;
Current_Y = 1;

while (count < projectedSimList.Count) : is 1 < 5? true
{
  GetReducedNew( count, sizeCheck);
  --sizeCheck;
  ++count;
}

GetReducedNew( count , sizeCheck )
{
  SimModel newSims = new SimModel();

  IF sizeCheck > Current_Y : is 4 > 1? true
  {
    newSims = projectedSimList[count];
    reducedProjected.Add(newSims);
  }
}

Second Run of Loop Results:

WAS:
reducedProjected.Count = 1;
projectedSimList.Count = 5;
sizeCheck = 4
count = 1;
Current_Y = 1;

NOW:
reducedProjected.Count = 2;
projectedSimList.Count = 5;
sizeCheck = 3
count = 2;
Current_Y = 1;

Third Run of Loop:

reducedProjected.Count = 2;
projectedSimList.Count = 5;
sizeCheck = 3
count = 2;
Current_Y = 1;

while (count < projectedSimList.Count) : is 2 < 5? true
{
  GetReducedNew( count, sizeCheck);
  --sizeCheck;
  ++count;
}

GetReducedNew( count , sizeCheck )
{
  SimModel newSims = new SimModel();

  IF sizeCheck > Current_Y : is 3 > 1? true
  {
    newSims = projectedSimList[count];
    reducedProjected.Add(newSims);
  }
}

Third Run of Loop Results:

WAS:
reducedProjected.Count = 2;
projectedSimList.Count = 5;
sizeCheck = 3
count = 2;
Current_Y = 1;

NOW:
reducedProjected.Count = 3;
projectedSimList.Count = 5;
sizeCheck = 2
count = 3;
Current_Y = 1;


Fourth Run of Loop:

reducedProjected.Count = 3;
projectedSimList.Count = 5;
sizeCheck = 2
count = 3;
Current_Y = 1;

while (count < projectedSimList.Count) : is 3 < 5? true
{
  GetReducedNew( count, sizeCheck);
  --sizeCheck;
  ++count;
}

GetReducedNew( count , sizeCheck )
{
  SimModel newSims = new SimModel();

  IF sizeCheck > Current_Y : is 2 > 1? true
  {
    newSims = projectedSimList[count];
    reducedProjected.Add(newSims);
  }
}

Third Run of Loop Results:

WAS:
reducedProjected.Count = 3;
projectedSimList.Count = 5;
sizeCheck = 2
count = 3;
Current_Y = 1;

NOW:
reducedProjected.Count = 4;
projectedSimList.Count = 5;
sizeCheck = 1
count = 4;
Current_Y = 1;

Fourth Run of Loop:

reducedProjected.Count = 4;
projectedSimList.Count = 5;
sizeCheck = 1
count = 4;
Current_Y = 1;

while (count < projectedSimList.Count) : is 4 < 5? true
{
  GetReducedNew( count, sizeCheck);
  --sizeCheck;
  ++count;
}

GetReducedNew( count , sizeCheck )
{
  SimModel newSims = new SimModel();

  IF sizeCheck > Current_Y : is 1 > 1? true
  {
    newSims = projectedSimList[count];
    reducedProjected.Add(newSims);
  }
}

Third Run of Loop Results:

WAS:
reducedProjected.Count = 4;
projectedSimList.Count = 5;
sizeCheck = 1
count = 4;
Current_Y = 1;

NOW:
reducedProjected.Count = 4;
projectedSimList.Count = 5;
sizeCheck = 0
count = 5;
Current_Y = 1;
